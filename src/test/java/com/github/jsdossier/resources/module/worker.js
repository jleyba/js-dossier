/**
 * @fileoverview This is the file overview comment.
 */

/**
 * And this is the exported constructor comment.
 * @param {string} name Worker name.
 * @constructor
 */
module.exports = function(name) {
};